<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BrainWave.APP.Views.DashboardPage"
             Title="Dashboard">
    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="12">
            <Label Text="Quick Navigation" FontAttributes="Bold" FontSize="20" />
            <Grid ColumnDefinitions="*,*,*,*" ColumnSpacing="8">
                <Button Text="Profile" Grid.Column="0" Clicked="Profile_Clicked" />
                <Button Text="Tasks" Grid.Column="1" Clicked="Tasks_Clicked" />
                <Button Text="Collab" Grid.Column="2" Clicked="Collab_Clicked" />
                <Button Text="Reminders" Grid.Column="3" Clicked="Reminders_Clicked" />
            </Grid>

            <Label Text="Closest Due Tasks" FontAttributes="Bold" FontSize="20" />
            <CollectionView x:Name="UpcomingList" ItemsSource="{Binding Upcoming}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Padding="12" CornerRadius="12" Margin="0,6">
                            <VerticalStackLayout>
                                <Label Text="{Binding Title}" FontAttributes="Bold" />
                                <Label Text="{Binding Description}" LineBreakMode="TailTruncation" />
                                <Label Text="{Binding Due_Date, StringFormat='Due: {0:yyyy-MM-dd HH:mm}'}" />
                                <HorizontalStackLayout>
                                    <Label Text="{Binding Priority_Level}" />
                                    <Label Text="•" />
                                    <Label Text="{Binding Task_Status}" />
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>